const priya = 'Asif Akbar';
const meya = "Meya tumi ki dukkho chino";
const kobita = `kobita tumi sopnop charini hoiya`;

const multiLineNew = `this is first line.
this is second line.
this is third line.
`;

const friends = ['abul', 'babul', 'kabul'];
const count = 5;
const oldVersion = '<h3 class="friend-name">Friend -4</h3>'
const oldVersion2 = '<h3 class="friend-name">Friend -' + count + '</h3>'

const newVersion = `<h3 class="friend-name">Friend -${count}</h3>`


const first = 'mamun';
const second = 'Chowdary';
const fullNew = `${first}  ${second}`;
const fullNew1 = `${first} bal koro ${second}`;
const fullNew2 = `${first} bal koro ${second} .He has ${friends.length * 500}`;

console.log(fullNew2);

const numbers = [10, 20, 30, 40, 50, 60, 70];
// Math.max(numbers);

console.log(Math.max(numbers));
